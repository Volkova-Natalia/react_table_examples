import React from "react";
import { css } from "styled-components/macro";


// --------------------------------------------------

export const StyledTableFixedFooter = css`
  .fixed-footer .tfoot {
    position: sticky;
    bottom: 0;
    z-index: 1;
  }
`;

// --------------------------------------------------
